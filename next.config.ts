import type { NextConfig } from "next";

// Baseline security headers (clickjacking, MIME-sniffing, referrer leakage,
// HTTPS enforcement). A Content-Security-Policy is deferred until third-party
// origins exist, so it can be authored against the real sources.
const securityHeaders = [
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "X-Frame-Options", value: "DENY" },
  {
    key: "Strict-Transport-Security",
    value: "max-age=63072000; includeSubDomains",
  },
];

const nextConfig: NextConfig = {
  async headers() {
    return [{ source: "/:path*", headers: securityHeaders }];
  },
};

export default nextConfig;
